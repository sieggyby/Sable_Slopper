"""
FastAPI web UI for sable-vault. Phase 2 feature — see docs/ROADMAP.md.
Runs via `sable serve`. Wraps the same Python functions called by the CLI.
Exposed via Cloudflare Tunnel for team/client access.
"""
